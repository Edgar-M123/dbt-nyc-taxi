
version: 2

models:
  - name: stg_yellow_trips
    description: "Staging yellow trip data"
    columns:
      - name: trip_id
        description: "The primary key for this table"
        data_tests:
          - unique
          - not_null

