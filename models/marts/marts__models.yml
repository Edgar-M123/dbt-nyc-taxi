
version: 2

models:
  - name: yellow_trips
    description: "Yellow taxi trip data"
    columns:
      - name: trip_id
        description: "The primary key for this table"
        data_tests:
          - unique
          - not_null
